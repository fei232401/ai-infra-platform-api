[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
addopts = -q --strict-markers
markers =
    integration: 需要可连接的 PostgreSQL 实例
filterwarnings =
    ignore::DeprecationWarning
